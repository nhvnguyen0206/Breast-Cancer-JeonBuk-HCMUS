# A21 focal-scale 2 — first 40 epochs

The selected checkpoint remains epoch 30 at Macro-F1 `0.550949`; epochs
31–40 do not improve it and epoch 40 is `0.520892`. Zero of 40 checkpoints
reach `0.75`.

A21 remains far below A5 fold0. Continue unchanged through epoch 50, then run
the complete screen audit and reject without confirmation unless the registered
gate is unexpectedly reached in the final ten epochs.
